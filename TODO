MAIN TASKS:
1) use root user by default or setup root user so the client can use sudo
2) actually encrypt container and not just copy the password in it
3) store prebuilt docker container with downloaded base packages so container creation is faster


SECONDARY TASKS:
1) link with sqlcypher and test
2) move errors to defines
3) list all containers user can access
4) implement container lock using usb key
5) create encrypted directory in container
6) stop container (somehow)
7) inject key only on container creation
8) create docker file for ubuntu and openssh
9) stop container when unused
