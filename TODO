### MAIN TASKS:
1) Store prebuilt docker container with downloaded base packages so container creation is faster
3) Use usb key

### For usb key:
1) load keys to usb (custom command)
2) read keys from usb (in client)
3) how secure would it be?
4) think about security if it is ok to just store keys on the (digital) key itself

### COMPLETED:
1) Change home directory permissions from root to client's user
2) Actually encrypt container and not just copy the password in it
3) Implement automatic container stop when user disconnects SSH
4) Add background cleanup thread for stopped containers
5) Fix SSH key setup to use encrypted volume instead of container overlay
6) Stop container when unused
7) Link with sqlcipher and test

### SECONDARY TASKS:
1) List all containers user can access (client side) [useless feature in my scope]

### TO WRITE:
1) key types that I use and how they work and all the math
2) LUKS and how it works
3) usb key algorithm
