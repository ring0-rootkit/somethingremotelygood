"""
Глава 4: Тестирование и результаты (~5 pages)
Adds Chapter 4 content to the document.
"""

from docx.shared import Pt, Cm
from docx.enum.text import WD_ALIGN_PARAGRAPH


def add_chapter(doc, heading_style, subheading_style, body_style, code_style, caption_style, table_style):
    """Add Chapter 4: Testing and Results."""

    doc.add_paragraph('ГЛАВА 4. ТЕСТИРОВАНИЕ И РЕЗУЛЬТАТЫ', heading_style)

    # ══════════════════════════════════════════════
    # 4.1 Методология тестирования
    # ══════════════════════════════════════════════
    doc.add_paragraph('4.1 Методология тестирования', subheading_style)

    doc.add_paragraph(
        'Тестирование системы проводилось на аппаратной конфигурации: модуль '
        'ESP32-C3-DevKitM-1 с 4 МБ flash, подключённый по USB к хост-машине '
        'под управлением Linux (ядро 6.8). Тестирование охватывало все компоненты '
        'системы: прошивку ESP32, Python-мост, Go-клиент и C-менеджер. Были '
        'разработаны сценарии функционального тестирования, тесты обратной '
        'совместимости, измерения производительности и анализ безопасности.',
        body_style
    )

    doc.add_paragraph(
        'Для каждого сценария фиксировались: входные данные, ожидаемый результат, '
        'фактический результат и статус (пройден/не пройден). Тестирование проводилось '
        'в ручном режиме через CLI-команды Makefile и утилиты keytool.',
        body_style
    )

    # ══════════════════════════════════════════════
    # 4.2 Функциональное тестирование
    # ══════════════════════════════════════════════
    doc.add_paragraph('4.2 Функциональное тестирование', subheading_style)

    doc.add_paragraph('4.2.1 Генерация ключей', subheading_style)

    doc.add_paragraph(
        'Сценарий Т1: Генерация ключа без пароля. Команда: make esp32-keygen '
        '(с пустым паролем). Ожидаемый результат: ESP32 генерирует ключевую пару, '
        'сохраняет plaintext seed и pubkey в NVS, возвращает успех. Фактический '
        'результат: keytool получает подтверждение генерации, make esp32-get-key '
        'выводит корректный ssh-ed25519 ключ. Статус: пройден.',
        body_style
    )

    doc.add_paragraph(
        'Сценарий Т2: Генерация ключа с паролем. Команда: make esp32-keygen '
        '(пароль «testpass123»). Ожидаемый результат: ESP32 генерирует ключевую '
        'пару, шифрует seed паролем, сохраняет enc_seed, salt, iv, pubkey и '
        'encrypted=1 в NVS, возвращает SSH_AGENT_SUCCESS. Фактический результат: '
        'keytool выводит «[+] Encrypted key generated successfully». Последующий '
        'make esp32-get-key возвращает открытый ключ (подтверждает, что pubkey '
        'хранится незашифрованным). Статус: пройден.',
        body_style
    )

    doc.add_paragraph(
        'Сценарий Т3: Повторная генерация ключа. Команда: make esp32-keygen '
        'после существующего ключа. Ожидаемый результат: новый ключ заменяет '
        'старый, при шифровании — plaintext privkey удаляется из NVS. '
        'Фактический результат: новый ключ сгенерирован, старый недоступен. '
        'Статус: пройден.',
        body_style
    )

    doc.add_paragraph('4.2.2 Блокировка и разблокировка', subheading_style)

    doc.add_paragraph(
        'Сценарий Т4: Разблокировка правильным паролем. Предусловие: ключ '
        'сгенерирован с паролем «testpass123», агент перезапущен (заблокирован). '
        'Команда: make esp32-unlock (пароль «testpass123»). Ожидаемый результат: '
        'ESP32 выполняет PBKDF2 (100000 итераций), расшифровывает seed, проверяет '
        'PKCS7-дополнение, возвращает SSH_AGENT_SUCCESS. Фактический результат: '
        '«[+] Agent unlocked successfully» после ~4 секунд ожидания (время PBKDF2). '
        'Статус: пройден.',
        body_style
    )

    doc.add_paragraph(
        'Сценарий Т5: Разблокировка неправильным паролем. Предусловие: ключ '
        'сгенерирован с паролем «testpass123». Команда: make esp32-unlock '
        '(пароль «wrongpassword»). Ожидаемый результат: PBKDF2 возвращает другой '
        'AES-ключ, расшифровка даёт мусорные данные, проверка PKCS7 не проходит, '
        'ESP32 возвращает SSH_AGENT_FAILURE. Фактический результат: '
        '«[-] Unlock failed (wrong password?)» после ~4 секунд. Закрытый ключ '
        'не загружен в память. Статус: пройден.',
        body_style
    )

    doc.add_paragraph(
        'Сценарий Т6: Блокировка агента. Предусловие: агент разблокирован. '
        'Команда: make esp32-lock. Ожидаемый результат: ESP32 очищает privateKey '
        'из RAM (memset(privateKey, 0, 32)), устанавливает keyUnlocked=false, '
        'возвращает SSH_AGENT_SUCCESS. Фактический результат: «[+] Agent locked». '
        'Последующая попытка подписи возвращает SSH_AGENT_LOCKED. Статус: пройден.',
        body_style
    )

    doc.add_paragraph(
        'Сценарий Т7: Подпись при заблокированном агенте. Предусловие: агент '
        'заблокирован (зашифрованный ключ). Команда: make esp32-sign '
        'CHALLENGE=0102...10. Ожидаемый результат: ESP32 возвращает '
        'SSH_AGENT_LOCKED (0x20). Фактический результат: keytool выводит '
        '«[!] Agent is locked. Unlock it first with --unlock». Статус: пройден.',
        body_style
    )

    doc.add_paragraph('4.2.3 Получение открытого ключа', subheading_style)

    doc.add_paragraph(
        'Сценарий Т8: Получение ключа при заблокированном агенте. Предусловие: '
        'ключ зашифрован, агент заблокирован. Команда: make esp32-get-key. '
        'Ожидаемый результат: ESP32 возвращает pubkey (хранится незашифрованным) '
        'независимо от состояния блокировки. Фактический результат: корректный '
        'ssh-ed25519 ключ выводится. Статус: пройден. Это подтверждает, что '
        'функция handleGetPublicKey() не проверяет keyUnlocked, а функция '
        'handleRequestIdentities() также работает при заблокированном агенте.',
        body_style
    )

    # Таблица 8 — Сводка результатов тестирования
    doc.add_paragraph(
        'Таблица 8 — Сводка результатов функционального тестирования',
        caption_style
    )
    table8 = doc.add_table(rows=9, cols=3, style=table_style)
    headers8 = ['Сценарий', 'Описание', 'Результат']
    for i, h in enumerate(headers8):
        table8.rows[0].cells[i].text = h
    test_data = [
        ('Т1', 'Генерация ключа без пароля', 'Пройден'),
        ('Т2', 'Генерация ключа с паролем', 'Пройден'),
        ('Т3', 'Повторная генерация ключа', 'Пройден'),
        ('Т4', 'Разблокировка правильным паролем', 'Пройден'),
        ('Т5', 'Разблокировка неправильным паролем', 'Пройден'),
        ('Т6', 'Блокировка агента', 'Пройден'),
        ('Т7', 'Подпись при заблокированном агенте', 'Пройден'),
        ('Т8', 'Получение ключа при заблокированном агенте', 'Пройден'),
    ]
    for row_idx, (tid, desc, result) in enumerate(test_data, 1):
        table8.rows[row_idx].cells[0].text = tid
        table8.rows[row_idx].cells[1].text = desc
        table8.rows[row_idx].cells[2].text = result

    doc.add_paragraph('')

    # ══════════════════════════════════════════════
    # 4.3 Тестирование обратной совместимости
    # ══════════════════════════════════════════════
    doc.add_paragraph('4.3 Тестирование обратной совместимости', subheading_style)

    doc.add_paragraph(
        'Обратная совместимость является важным требованием системы: ключи, '
        'сгенерированные без пароля (версия 1.x), должны продолжать работать '
        'в прошивке версии 2.0.0 без изменений.',
        body_style
    )

    doc.add_paragraph(
        'Сценарий Т9: Работа с незашифрованным ключом в прошивке 2.0.0. '
        'Предусловие: ключ сгенерирован без пароля (encrypted=0 в NVS). '
        'Действия: перезагрузка ESP32, make esp32-sign CHALLENGE=..., '
        'make esp32-get-key, запуск agent-bridge и подключение через '
        'client-run-agent. Ожидаемый результат: все операции работают без '
        'запроса пароля. Фактический результат: при загрузке выводится '
        '«Key loaded from storage» (без «locked»), подписание работает '
        'немедленно, SSH-подключение успешно. Статус: пройден.',
        body_style
    )

    doc.add_paragraph(
        'Сценарий Т10: Переход от незашифрованного к зашифрованному ключу. '
        'Предусловие: ключ без пароля. Действие: make esp32-keygen с паролем. '
        'Ожидаемый результат: новый зашифрованный ключ заменяет старый, '
        'plaintext privkey удаляется из NVS (prefs.remove(\"privkey\")). '
        'Фактический результат: после генерации plaintext ключ недоступен, '
        'только зашифрованный. Статус: пройден.',
        body_style
    )

    doc.add_paragraph(
        'Сценарий Т11: Работа Go-клиента с RSA-ключом (без ESP32). '
        'Предусловие: зарегистрирован пользователь с RSA-ключом. '
        'Действие: ./build/client localhost 5555 bob bob keys/_private.pem. '
        'Ожидаемый результат: аутентификация по RSA, подключение к контейнеру. '
        'Фактический результат: RSA-подпись (PKCS1v15 + SHA256) создаётся '
        'локально (crypto.go), менеджер верифицирует через verify_rsa(), '
        'SSH-сессия открывается с временным OpenSSH-ключом. Статус: пройден.',
        body_style
    )

    # ══════════════════════════════════════════════
    # 4.4 Измерения производительности
    # ══════════════════════════════════════════════
    doc.add_paragraph('4.4 Измерения производительности', subheading_style)

    doc.add_paragraph(
        'Были проведены измерения времени выполнения критических операций '
        'на ESP32-C3 (RISC-V, 160 МГц). Измерения проводились путём замера '
        'времени от отправки команды до получения ответа на стороне хоста.',
        body_style
    )

    # Таблица 9 — Производительность
    doc.add_paragraph(
        'Таблица 9 — Результаты измерений производительности',
        caption_style
    )
    table9 = doc.add_table(rows=7, cols=3, style=table_style)
    headers9 = ['Операция', 'Время (сек)', 'Примечание']
    for i, h in enumerate(headers9):
        table9.rows[0].cells[i].text = h
    perf_data = [
        ('Генерация ключа Ed25519', '~0.3', 'TRNG + SHA-512 + scalarbase'),
        ('Подпись Ed25519 (32B challenge)', '~0.2', 'SHA-512 + 2x scalarbase + modL'),
        ('PBKDF2-SHA256 (100k итераций)', '~3.5', 'Основная задержка при разблокировке'),
        ('AES-256-CBC шифрование (48B)', '<0.01', 'Незначительно по сравнению с PBKDF2'),
        ('Полная разблокировка', '~3.5', 'PBKDF2 + AES decrypt + PKCS7 verify'),
        ('Полный цикл аутентификации', '~4.0', 'Разблокировка + подпись + SSH handshake'),
    ]
    for row_idx, (op, time_val, note) in enumerate(perf_data, 1):
        table9.rows[row_idx].cells[0].text = op
        table9.rows[row_idx].cells[1].text = time_val
        table9.rows[row_idx].cells[2].text = note

    doc.add_paragraph('')

    doc.add_paragraph(
        'Основным узким местом производительности является PBKDF2 с 100000 итераций, '
        'занимающий около 3.5 секунд на ESP32-C3. Это время является приемлемым для '
        'пользователя (операция разблокировки выполняется однократно), но создаёт '
        'значительный барьер для атак перебором. Подписание Ed25519 выполняется '
        'быстро (~0.2 секунды), что не создаёт заметных задержек при SSH-аутентификации.',
        body_style
    )

    doc.add_paragraph(
        'Латентность серийного соединения (USB CDC @ 115200 бод) незначительна для '
        'размеров передаваемых данных: максимальный пакет (65 байт для подписи) '
        'передаётся за ~5.6 мс. Основная задержка при обычном использовании (агент '
        'уже разблокирован) составляет ~0.2 секунды (время подписания), что '
        'незначительно для пользователя.',
        body_style
    )

    # ══════════════════════════════════════════════
    # 4.5 Анализ безопасности
    # ══════════════════════════════════════════════
    doc.add_paragraph('4.5 Анализ безопасности', subheading_style)

    doc.add_paragraph('4.5.1 Сценарий: кража ESP32', subheading_style)

    doc.add_paragraph(
        'При физическом завладении устройством ESP32 злоумышленник может: '
        '(1) считать содержимое flash-памяти через JTAG или esptool; '
        '(2) извлечь данные из NVS-раздела. Если ключ зашифрован паролем, '
        'злоумышленник получит: enc_seed (48 байт), salt (16 байт), iv (16 байт), '
        'pubkey (32 байт), encrypted=1. Для получения закрытого ключа необходимо '
        'атаковать PBKDF2-SHA256 с 100000 итераций.',
        body_style
    )

    doc.add_paragraph(
        'Оценка стойкости к перебору: при скорости 100000 PBKDF2 итераций на '
        'одну проверку пароля, специализированный GPU (NVIDIA RTX 4090) способен '
        'выполнять приблизительно 200000-500000 SHA-256 хешей в секунду для PBKDF2 '
        'с 100000 итераций. Для пароля из 8 строчных латинских букв (26^8 ≈ 2×10^11 '
        'комбинаций) перебор займёт: 2×10^11 / 500000 ≈ 400000 секунд ≈ 4.6 дня. '
        'Для пароля из 10 символов (с цифрами и спецсимволами, 94^10 ≈ 5×10^19): '
        'перебор практически невозможен.',
        body_style
    )

    doc.add_paragraph(
        'Если ключ не зашифрован паролем (encrypted=0), злоумышленник получает '
        'plaintext seed из NVS, что даёт полный доступ к закрытому ключу. Поэтому '
        'для использования в продуктивных средах настоятельно рекомендуется '
        'генерировать ключ с паролем.',
        body_style
    )

    doc.add_paragraph('4.5.2 Сценарий: компрометация хост-машины', subheading_style)

    doc.add_paragraph(
        'При компрометации хост-машины (малварь, root-доступ) злоумышленник может: '
        '(1) перехватить трафик по Unix-сокету; (2) подключиться к сокету агента и '
        'запросить подпись произвольных данных; (3) перехватить пароль при вводе '
        '(keylogger). Однако злоумышленник не может: (1) извлечь закрытый ключ — '
        'он никогда не передаётся по сокету; (2) подписать данные после блокировки '
        'агента — для разблокировки нужен пароль; (3) модифицировать прошивку ESP32 '
        'по USB без физического доступа к кнопке BOOT.',
        body_style
    )

    doc.add_paragraph(
        'Ограничение: в текущей реализации мост не проверяет подлинность запросов '
        'через Unix-сокет (нет аутентификации клиента). Любой процесс, имеющий '
        'доступ к файлу /tmp/esp32-agent.sock (права 0600), может отправлять '
        'запросы на подпись. Это аналогично поведению стандартного ssh-agent и '
        'является общепринятым компромиссом.',
        body_style
    )

    doc.add_paragraph('4.5.3 Сценарий: атака на серийный протокол', subheading_style)

    doc.add_paragraph(
        'Серийный протокол между хостом и ESP32 не шифруется и не аутентифицируется. '
        'Теоретически, при физическом доступе к USB-кабелю возможна MITM-атака. '
        'Однако в типичном сценарии использования (ESP32 подключён напрямую к '
        'ноутбуку) эта угроза минимальна. Кроме того, закрытый ключ не передаётся '
        'по серийному каналу — передаются только данные для подписания и '
        'результирующая подпись.',
        body_style
    )

    doc.add_paragraph('4.5.4 Очистка секретных данных из памяти', subheading_style)

    doc.add_paragraph(
        'Прошивка ESP32 обеспечивает очистку секретных данных после использования: '
        '(1) AES-ключ (aesKey) обнуляется memset после деривации и использования; '
        '(2) расшифрованный seed обнуляется в decryptSeed() после копирования; '
        '(3) промежуточный буфер padded обнуляется в encryptSeed(); '
        '(4) при блокировке (handleLock) приватный ключ обнуляется: '
        'memset(privateKey, 0, 32). Это предотвращает утечку секретных данных '
        'через незащищённые области SRAM при перезагрузке или сбое.',
        body_style
    )

    # Таблица 10 — Матрица угроз
    doc.add_paragraph(
        'Таблица 10 — Матрица угроз и контрмер',
        caption_style
    )
    table10 = doc.add_table(rows=6, cols=3, style=table_style)
    headers10 = ['Угроза', 'Контрмера', 'Остаточный риск']
    for i, h in enumerate(headers10):
        table10.rows[0].cells[i].text = h
    threats = [
        ('Кража ESP32', 'PBKDF2+AES шифрование seed', 'Низкий (при сильном пароле)'),
        ('Компрометация хоста', 'Ключ не покидает ESP32', 'Средний (подпись по запросу)'),
        ('Перебор пароля', '100k итераций PBKDF2', 'Низкий (8+ символов)'),
        ('MITM на USB', 'Нет (не реализовано)', 'Низкий (физ. доступ)'),
        ('Утечка из RAM', 'memset после использования', 'Низкий'),
    ]
    for row_idx, (threat, countermeasure, risk) in enumerate(threats, 1):
        table10.rows[row_idx].cells[0].text = threat
        table10.rows[row_idx].cells[1].text = countermeasure
        table10.rows[row_idx].cells[2].text = risk

    doc.add_paragraph('')

    doc.add_paragraph(
        'Выводы по главе: функциональное тестирование подтвердило корректность '
        'работы всех компонентов системы. Все 11 тестовых сценариев пройдены '
        'успешно. Обратная совместимость с незашифрованными ключами подтверждена. '
        'Производительность ESP32-C3 достаточна для комфортного использования: '
        'подпись занимает ~0.2 секунды, разблокировка — ~3.5 секунды. Анализ '
        'безопасности показал, что система обеспечивает адекватную защиту при '
        'использовании сильных паролей, хотя и уступает коммерческим решениям '
        'с аппаратным защищённым элементом.',
        body_style
    )
