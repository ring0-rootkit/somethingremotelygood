"""
Глава 2: Проектирование и архитектура (~10 pages)
Adds Chapter 2 content to the document.
"""

from docx.shared import Pt, Cm
from docx.enum.text import WD_ALIGN_PARAGRAPH


def add_chapter(doc, heading_style, subheading_style, body_style, code_style, caption_style, table_style):
    """Add Chapter 2: Design and Architecture."""

    doc.add_paragraph('ГЛАВА 2. ПРОЕКТИРОВАНИЕ И АРХИТЕКТУРА СИСТЕМЫ', heading_style)

    # ══════════════════════════════════════════════
    # 2.1 Общая архитектура системы
    # ══════════════════════════════════════════════
    doc.add_paragraph('2.1 Общая архитектура системы', subheading_style)

    doc.add_paragraph(
        'Разрабатываемая система представляет собой многокомпонентное решение для '
        'безопасного удалённого доступа к изолированным контейнерным средам с использованием '
        'аппаратного хранения криптографических ключей. Система состоит из пяти основных '
        'компонентов, взаимодействующих между собой по различным протоколам.',
        body_style
    )

    doc.add_paragraph(
        'Первый компонент — микроконтроллер ESP32-C3, выполняющий роль аппаратного '
        'модуля безопасности (HSM). ESP32 хранит закрытый ключ Ed25519 в энергонезависимой '
        'памяти NVS, генерирует ключевые пары и выполняет операции цифровой подписи. '
        'Закрытый ключ может быть зашифрован паролем пользователя с использованием '
        'AES-256-CBC и PBKDF2-SHA256 для деривации ключа шифрования.',
        body_style
    )

    doc.add_paragraph(
        'Второй компонент — Python-мост (bridge), реализующий трансляцию между '
        'протоколом SSH-агента и собственным серийным протоколом ESP32. Мост слушает '
        'Unix-сокет и выступает в роли SSH-агента для стандартных SSH-клиентов, '
        'перенаправляя запросы на подпись к ESP32 через USB-serial соединение.',
        body_style
    )

    doc.add_paragraph(
        'Третий компонент — Go-клиент, обеспечивающий аутентификацию пользователя '
        'на сервере управления по протоколу challenge-response с использованием '
        'ESP32-агента для подписания challenge. Клиент также отвечает за запрос пароля '
        'разблокировки у пользователя при обнаружении заблокированного агента и '
        'установку SSH-сессии в контейнер после успешной аутентификации.',
        body_style
    )

    doc.add_paragraph(
        'Четвёртый компонент — C-менеджер (manager), серверная часть системы, '
        'выполняющая аутентификацию пользователей, управление контейнерами LXD, '
        'создание и монтирование зашифрованных LUKS-томов, настройку SSH-доступа '
        'внутри контейнеров и мониторинг активных сессий. Менеджер использует '
        'зашифрованную базу данных SQLCipher для хранения информации о пользователях, '
        'их открытых ключах и ключах шифрования контейнеров.',
        body_style
    )

    doc.add_paragraph(
        'Пятый компонент — контейнеры LXD, предоставляющие изолированные рабочие '
        'среды для пользователей. Каждый контейнер основан на образе Alpine Linux 3.20, '
        'имеет собственный зашифрованный домашний каталог и доступен по SSH через '
        'проброс портов.',
        body_style
    )

    # Рисунок 1 — Архитектурная диаграмма (текстовая)
    doc.add_paragraph(
        'Рисунок 1 — Общая архитектура системы',
        caption_style
    )
    code = doc.add_paragraph('', code_style)
    code.text = (
        '+-----------+   USB/Serial   +-------------+   Unix Socket   +----------+\n'
        '|  ESP32-C3 |<-------------->| Python      |<--------------->| Go       |\n'
        '|  (HSM)    |  115200 baud   | Bridge      |  SSH Agent      | Client   |\n'
        '|           |                | (agent)     |  Protocol       |          |\n'
        '| Ed25519   |                |             |                 |          |\n'
        '| NVS Store |                | /tmp/esp32- |                 |          |\n'
        '| AES-256   |                | agent.sock  |                 |          |\n'
        '+-----------+                +-------------+                 +----+-----+\n'
        '                                                                  |\n'
        '                                                            TCP :5555\n'
        '                                                                  |\n'
        '                                                             +----+-----+\n'
        '                                                             | C        |\n'
        '                                                             | Manager  |\n'
        '                                                             |          |\n'
        '                                                             | SQLCipher|\n'
        '                                                             | LUKS     |\n'
        '                                                             +----+-----+\n'
        '                                                                  |\n'
        '                                                              LXD API\n'
        '                                                                  |\n'
        '                                                             +----+-----+\n'
        '                                                             | LXD      |\n'
        '                                                             | Container|\n'
        '                                                             | Alpine   |\n'
        '                                                             | 3.20     |\n'
        '                                                             +----------+'
    )

    doc.add_paragraph('')

    doc.add_paragraph(
        'Взаимодействие компонентов происходит по следующей цепочке: (1) Go-клиент '
        'подключается к Unix-сокету Python-моста и отправляет запросы в формате '
        'протокола SSH-агента; (2) Python-мост транслирует запросы в собственный '
        'серийный протокол ESP32 и передаёт через USB; (3) ESP32 выполняет '
        'криптографическую операцию и возвращает результат; (4) Go-клиент использует '
        'подпись для аутентификации на C-менеджере по TCP; (5) менеджер создаёт '
        'контейнер, монтирует зашифрованный том и возвращает номер SSH-порта; '
        '(6) Go-клиент устанавливает SSH-сессию в контейнер, используя ESP32-агент '
        'для аутентификации по ключу.',
        body_style
    )

    # ══════════════════════════════════════════════
    # 2.2 Проектирование протоколов
    # ══════════════════════════════════════════════
    doc.add_paragraph('2.2 Проектирование протоколов взаимодействия', subheading_style)

    doc.add_paragraph('2.2.1 Собственный серийный протокол ESP32', subheading_style)

    doc.add_paragraph(
        'Для взаимодействия с ESP32 через USB-serial соединение был разработан '
        'компактный бинарный протокол. Протокол использует асимметричный формат '
        'сообщений: формат запроса (от хоста к ESP32) отличается от формата ответа '
        '(от ESP32 к хосту).',
        body_style
    )

    doc.add_paragraph(
        'Формат запроса (хост → ESP32): [type: 1 байт][length: 3 байта, big-endian]'
        '[data: length байт]. Первый байт определяет тип команды, следующие три '
        'байта — длину данных (максимум 16 МБ, практически используется до 4096 байт). '
        'Такой формат обеспечивает минимальный overhead — заголовок всего 4 байта.',
        body_style
    )

    doc.add_paragraph(
        'Формат ответа (ESP32 → хост): [length: 4 байта, big-endian][type: 1 байт]'
        '[payload: (length-1) байт]. Четырёхбайтовый заголовок длины включает '
        'байт типа. Первый байт данных определяет тип ответа. Такой формат '
        'унифицирован с протоколом SSH-агента, который также использует 4-байтовый '
        'заголовок длины.',
        body_style
    )

    # Таблица 5 — типы сообщений серийного протокола
    doc.add_paragraph(
        'Таблица 5 — Типы сообщений серийного протокола ESP32',
        caption_style
    )
    table5 = doc.add_table(rows=10, cols=4, style=table_style)
    headers5 = ['Код', 'Имя', 'Направление', 'Описание']
    for i, h in enumerate(headers5):
        table5.rows[0].cells[i].text = h
    proto_msgs = [
        ('0x01', 'PROTOCOL_SIGN_CHALLENGE', 'Хост → ESP32', 'Подпись произвольных данных'),
        ('0x02', 'PROTOCOL_GET_PUBLIC_KEY', 'Хост → ESP32', 'Запрос открытого ключа'),
        ('0x03', 'PROTOCOL_UNLOCK', 'Хост → ESP32', 'Разблокировка (данные = пароль)'),
        ('0x04', 'PROTOCOL_LOCK', 'Хост → ESP32', 'Блокировка (данные пусты)'),
        ('0x05', 'PROTOCOL_GENERATE_ENCRYPTED', 'Хост → ESP32', 'Генерация ключа с паролем'),
        ('0x05', 'SSH_AGENT_FAILURE', 'ESP32 → Хост', 'Ошибка выполнения операции'),
        ('0x06', 'SSH_AGENT_SUCCESS', 'ESP32 → Хост', 'Операция выполнена успешно'),
        ('0x0E', 'SSH_AGENT_SIGN_RESPONSE', 'ESP32 → Хост', 'Ответ с подписью (64 байта)'),
        ('0x20', 'SSH_AGENT_LOCKED', 'ESP32 → Хост', 'Агент заблокирован'),
    ]
    for row_idx, (code_val, name, direction, desc) in enumerate(proto_msgs, 1):
        table5.rows[row_idx].cells[0].text = code_val
        table5.rows[row_idx].cells[1].text = name
        table5.rows[row_idx].cells[2].text = direction
        table5.rows[row_idx].cells[3].text = desc

    doc.add_paragraph('')

    doc.add_paragraph(
        'Важной особенностью реализации является необходимость фильтрации отладочного '
        'текста, который ESP32 выводит через Serial.println(). Поскольку отладочные '
        'сообщения передаются по тому же UART-каналу, что и бинарный протокол, '
        'принимающая сторона (Python-мост и keytool) должна пропускать байты ASCII-текста '
        'до обнаружения валидного заголовка пакета. Заголовок ответа всегда начинается '
        'с байта 0x00 (старший байт длины), поскольку длина пакета не превышает 4096 '
        'байт (0x00001000), а ASCII-символы имеют значения от 0x20 до 0x7E. Это позволяет '
        'надёжно отличить начало бинарного пакета от отладочного текста.',
        body_style
    )

    doc.add_paragraph('2.2.2 Интеграция с протоколом SSH-агента', subheading_style)

    doc.add_paragraph(
        'Python-мост реализует серверную сторону протокола SSH-агента, принимая '
        'подключения на Unix-сокете /tmp/esp32-agent.sock. Для каждого входящего '
        'сообщения от SSH-клиента мост выполняет трансляцию в серийный протокол ESP32 '
        'и обратно.',
        body_style
    )

    doc.add_paragraph(
        'Трансляция запроса SSH_AGENTC_REQUEST_IDENTITIES (тип 11): мост пересылает '
        'запрос ESP32, получает 32-байтовый открытый ключ и формирует ответ '
        'SSH_AGENT_IDENTITIES_ANSWER. При этом 32-байтовый «сырой» ключ Ed25519 '
        'оборачивается в формат SSH wire format key blob: [4-byte len][\"ssh-ed25519\"]'
        '[4-byte len][32-byte key]. К ответу добавляется комментарий «ESP32-SSH-Agent».',
        body_style
    )

    doc.add_paragraph(
        'Трансляция запроса SSH_AGENTC_SIGN_REQUEST (тип 13): мост извлекает '
        '«сырой» 32-байтовый ключ из SSH key blob, формирует запрос для ESP32 '
        'и получает 64-байтовую подпись Ed25519. Подпись оборачивается в формат '
        'SSH wire format signature blob: [4-byte len][\"ssh-ed25519\"][4-byte len]'
        '[64-byte signature]. Если ESP32 возвращает SSH_AGENT_LOCKED (0x20), мост '
        'пробрасывает этот статус клиенту без изменений.',
        body_style
    )

    doc.add_paragraph(
        'Для операций блокировки и разблокировки используется механизм расширений '
        'SSH-агента (SSH_AGENTC_EXTENSION, тип 27). Go-клиент отправляет расширение '
        'с именем «esp32-unlock» и паролем в формате SSH-строки, или расширение '
        '«esp32-lock» без данных. Мост разбирает расширение, извлекает пароль и '
        'отправляет соответствующую команду ESP32 по серийному протоколу.',
        body_style
    )

    doc.add_paragraph('2.2.3 Протокол аутентификации клиент-менеджер', subheading_style)

    doc.add_paragraph(
        'Взаимодействие между Go-клиентом и C-менеджером осуществляется по TCP '
        'на порту 5555 и включает следующие этапы: (1) клиент отправляет user_id '
        '(текстовая строка); (2) менеджер генерирует 32-байтовый challenge с помощью '
        'OpenSSL RAND_bytes и отправляет его клиенту; (3) клиент подписывает challenge '
        'с помощью ESP32-агента (Ed25519) и отправляет подпись; (4) менеджер загружает '
        'открытый ключ пользователя из SQLCipher и проверяет подпись; (5) при успешной '
        'проверке менеджер отправляет «REQ CID»; (6) клиент отправляет container_id; '
        '(7) менеджер создаёт/запускает контейнер, монтирует LUKS-том, настраивает SSH '
        'и отправляет «OK» и номер SSH-порта.',
        body_style
    )

    # ══════════════════════════════════════════════
    # 2.3 Модель безопасности
    # ══════════════════════════════════════════════
    doc.add_paragraph('2.3 Модель безопасности', subheading_style)

    doc.add_paragraph(
        'Модель безопасности системы основана на многоуровневой защите (defense in '
        'depth), где компрометация одного уровня не приводит к полной компрометации '
        'системы.',
        body_style
    )

    doc.add_paragraph(
        'Первый уровень — аппаратное хранение ключей. Закрытый ключ Ed25519 хранится '
        'исключительно на микроконтроллере ESP32 и не передаётся на хост-машину. '
        'Все операции подписания выполняются на ESP32, а хост получает только '
        'результат — цифровую подпись. Это означает, что компрометация хост-машины '
        '(вирус, руткит, перехват памяти) не позволяет извлечь закрытый ключ.',
        body_style
    )

    doc.add_paragraph(
        'Второй уровень — шифрование закрытого ключа паролем. Даже при физическом '
        'доступе к ESP32 и извлечении содержимого flash-памяти, злоумышленник получит '
        'только зашифрованный seed (48 байт), соль (16 байт) и IV (16 байт). Для '
        'расшифровки необходимо знать пароль пользователя. При 100000 итераций PBKDF2 '
        'атака полным перебором на одном GPU (SHA-256, ~10 Ghash/s для несолёного хеша) '
        'снижается до ~100000 попыток в секунду, что делает перебор паролей длиной 8+ '
        'символов непрактичным.',
        body_style
    )

    doc.add_paragraph(
        'Третий уровень — механизм блокировки/разблокировки. После расшифровки '
        'закрытый ключ хранится в оперативной памяти ESP32 только до тех пор, '
        'пока агент разблокирован. Команда LOCK очищает закрытый ключ из RAM '
        '(memset(privateKey, 0, 32)), после чего все операции подписания возвращают '
        'SSH_AGENT_LOCKED. Отключение питания ESP32 также автоматически блокирует '
        'агент, поскольку содержимое SRAM теряется.',
        body_style
    )

    doc.add_paragraph(
        'Четвёртый уровень — шифрование данных контейнеров. Домашний каталог каждого '
        'пользователя хранится в зашифрованном LUKS-томе. Ключ шифрования тома (32 байта) '
        'хранится в зашифрованной базе данных SQLCipher, защищённой мастер-паролем '
        '(переменная окружения DB_PASSWORD). Том расшифровывается только на время '
        'активной сессии пользователя и автоматически закрывается при отключении '
        '(lxd_stop_container вызывает volume_close_encrypted_home).',
        body_style
    )

    doc.add_paragraph(
        'Пятый уровень — изоляция контейнеров. Каждый пользователь работает в отдельном '
        'LXD-контейнере с собственным сетевым стеком, файловой системой и пространством '
        'процессов. Доступ по SSH с аутентификацией только по открытому ключу '
        '(PasswordAuthentication no). Пароль учётной записи в контейнере заблокирован.',
        body_style
    )

    # Рисунок 2 — Поток разблокировки
    doc.add_paragraph(
        'Рисунок 2 — Последовательность разблокировки агента и подписания',
        caption_style
    )
    code2 = doc.add_paragraph('', code_style)
    code2.text = (
        'Go Client          Python Bridge         ESP32\n'
        '    |                    |                   |\n'
        '    |--- SIGN_REQUEST ->|                   |\n'
        '    |                   |-- SIGN_REQ(0x0D)->|\n'
        '    |                   |                   | (key encrypted,\n'
        '    |                   |<- LOCKED(0x20) ---|  not unlocked)\n'
        '    |<-- LOCKED(0x20) --|                   |\n'
        '    |                                       |\n'
        '    | [prompt password]                     |\n'
        '    |                                       |\n'
        '    |--- EXTENSION ---->|                   |\n'
        '    | "esp32-unlock"    |                   |\n'
        '    | + password        |-- UNLOCK(0x03) -->|\n'
        '    |                   |  + password       | PBKDF2(100k)\n'
        '    |                   |                   | AES-256 decrypt\n'
        '    |                   |                   | verify PKCS7\n'
        '    |                   |<- SUCCESS(0x06) --|\n'
        '    |<-- SUCCESS -------|                   |\n'
        '    |                                       |\n'
        '    |--- SIGN_REQUEST ->|                   |\n'
        '    |                   |-- SIGN_REQ(0x0D)->|\n'
        '    |                   |                   | Ed25519 sign\n'
        '    |                   |<- SIGN_RESP(0x0E)-|\n'
        '    |<-- SIGN_RESP -----|                   |'
    )

    doc.add_paragraph('')

    # ══════════════════════════════════════════════
    # 2.4 Диаграммы потоков данных
    # ══════════════════════════════════════════════
    doc.add_paragraph('2.4 Диаграммы потоков данных', subheading_style)

    doc.add_paragraph('2.4.1 Генерация зашифрованного ключа', subheading_style)

    doc.add_paragraph(
        'Процесс генерации зашифрованного ключа инициируется пользователем через '
        'команду make esp32-keygen. Makefile запрашивает пароль с помощью read -sp '
        'и передаёт его утилите esp32_keytool.py с флагом --password. Keytool отправляет '
        'команду PROTOCOL_GENERATE_ENCRYPTED (0x05) с паролем в качестве данных.',
        body_style
    )

    doc.add_paragraph(
        'На стороне ESP32 функция handleGenerateEncrypted() вызывает generateEncryptedKey(), '
        'которая выполняет следующие шаги: (1) генерация 32-байтового seed с помощью '
        'аппаратного ГСЧ (esp_random()); (2) вычисление открытого ключа Ed25519 из seed; '
        '(3) генерация 16-байтовой случайной соли; (4) генерация 16-байтового случайного '
        'IV; (5) деривация 32-байтового AES-ключа из пароля и соли через PBKDF2-SHA256 '
        '(100000 итераций); (6) PKCS7-дополнение seed до 48 байт; (7) шифрование '
        'дополненного seed с помощью AES-256-CBC; (8) сохранение pubkey, enc_seed, salt, '
        'iv и флага encrypted=1 в NVS; (9) удаление plaintext privkey из NVS (если '
        'существовал); (10) очистка seed из RAM.',
        body_style
    )

    # Рисунок 3 — Генерация ключа
    doc.add_paragraph(
        'Рисунок 3 — Поток данных при генерации зашифрованного ключа',
        caption_style
    )
    code3 = doc.add_paragraph('', code_style)
    code3.text = (
        'Пользователь     Keytool (Python)        ESP32\n'
        '    |                  |                     |\n'
        '    |-- password ----->|                     |\n'
        '    |                  |--- 0x05 + pwd ----->|\n'
        '    |                  |                     | seed = esp_random(32)\n'
        '    |                  |                     | pk = ed25519_keypair(seed)\n'
        '    |                  |                     | salt = esp_random(16)\n'
        '    |                  |                     | iv   = esp_random(16)\n'
        '    |                  |                     | aesKey = PBKDF2(pwd, salt, 100k)\n'
        '    |                  |                     | enc = AES-CBC(seed||pad, aesKey, iv)\n'
        '    |                  |                     | NVS: pubkey, enc_seed, salt, iv,\n'
        '    |                  |                     |      encrypted=1\n'
        '    |                  |                     | memset(seed, 0)\n'
        '    |                  |<-- SUCCESS(0x06) ---|\n'
        '    |<-- "OK" --------|                     |'
    )

    doc.add_paragraph('')

    doc.add_paragraph('2.4.2 Полный цикл аутентификации и подключения', subheading_style)

    doc.add_paragraph(
        'Полный цикл аутентификации пользователя с использованием ESP32-агента '
        'и подключения к контейнеру включает следующие этапы, показанные на Рисунке 4.',
        body_style
    )

    doc.add_paragraph(
        'Рисунок 4 — Полный цикл аутентификации и подключения к контейнеру',
        caption_style
    )
    code4 = doc.add_paragraph('', code_style)
    code4.text = (
        'Go Client      Bridge      ESP32     Manager      LXD\n'
        '    |             |          |          |           |\n'
        '    |===== TCP connect ================>|           |\n'
        '    |----------user_id----------------->|           |\n'
        '    |<---------challenge(32B)-----------|           |\n'
        '    |                                               |\n'
        '    |-- SIGN_REQ -->|                               |\n'
        '    |               |-- 0x0D -->|                   |\n'
        '    |               |<- 0x20 --|  (LOCKED)         |\n'
        '    |<-- LOCKED ----|                               |\n'
        '    | [password prompt]                             |\n'
        '    |-- EXTENSION ->|                               |\n'
        '    | esp32-unlock  |-- 0x03 -->|                   |\n'
        '    |               |<- 0x06 --|  (SUCCESS)        |\n'
        '    |<-- SUCCESS ---|                               |\n'
        '    |-- SIGN_REQ -->|                               |\n'
        '    |               |-- 0x0D -->|                   |\n'
        '    |               |<- 0x0E --|  (signature)      |\n'
        '    |<-- SIGN_RESP -|                               |\n'
        '    |                                               |\n'
        '    |----------signature(64B)---------->|           |\n'
        '    |                                   | verify()  |\n'
        '    |<----------"REQ CID"---------------|           |\n'
        '    |----------container_id------------>|           |\n'
        '    |                                   |-- create->|\n'
        '    |                                   |  LUKS open|\n'
        '    |                                   |  mount    |\n'
        '    |                                   |  SSH setup|\n'
        '    |                                   |<-- port --|\n'
        '    |<---------"OK" + port -------------|           |\n'
        '    |                                               |\n'
        '    |=== SSH connect (using agent) ================>|'
    )

    doc.add_paragraph('')

    # ══════════════════════════════════════════════
    # 2.5 Выбор технологий и обоснование
    # ══════════════════════════════════════════════
    doc.add_paragraph('2.5 Выбор технологий и обоснование', subheading_style)

    doc.add_paragraph('2.5.1 Микроконтроллер ESP32-C3', subheading_style)

    doc.add_paragraph(
        'Для реализации аппаратного модуля безопасности был выбран микроконтроллер '
        'ESP32-C3 производства Espressif Systems. Выбор обусловлен следующими '
        'факторами: (1) архитектура RISC-V с тактовой частотой 160 МГц, обеспечивающая '
        'достаточную производительность для криптографических операций; (2) встроенная '
        'поддержка USB через контроллер USB Serial/JTAG, что устраняет необходимость '
        'во внешнем преобразователе USB-UART; (3) 400 КБ SRAM и 4 МБ flash-памяти; '
        '(4) аппаратный генератор истинно случайных чисел (TRNG) на основе теплового '
        'шума; (5) встроенная библиотека mbedTLS в составе ESP-IDF, включающая '
        'реализации AES, SHA-256, PBKDF2; (6) крайне низкая стоимость — модуль '
        'ESP32-C3-DevKitM-1 стоит около 3 USD [13].',
        body_style
    )

    doc.add_paragraph(
        'Конфигурация PlatformIO для проекта определена в файле platformio.ini. '
        'Используется плата esp32-c3-devkitm-1, фреймворк Arduino (для упрощения '
        'разработки), скорость Serial — 115200 бод, режим flash — DIO. Единственная '
        'внешняя зависимость — библиотека Preferences (обёртка над NVS). Криптографические '
        'функции mbedTLS (pkcs5.h, aes.h, md.h) доступны из ESP-IDF без дополнительных '
        'зависимостей. Отладочные сообщения отключены (CORE_DEBUG_LEVEL=0) для минимизации '
        'помех бинарному протоколу.',
        body_style
    )

    doc.add_paragraph('2.5.2 Python для моста и утилиты управления ключами', subheading_style)

    doc.add_paragraph(
        'Python был выбран для реализации SSH-агентного моста и утилиты управления '
        'ключами (keytool) по следующим причинам: (1) быстрая разработка и прототипирование — '
        'Python позволяет реализовать сложный сетевой и серийный протокол в компактном '
        'коде; (2) кроссплатформенность — библиотека pyserial работает одинаково на Linux, '
        'macOS и Windows; (3) удобная работа с бинарными данными через модуль struct; '
        '(4) встроенная поддержка Unix-сокетов и многопоточности; (5) широкая '
        'распространённость — Python предустановлен на большинстве Linux-систем.',
        body_style
    )

    doc.add_paragraph(
        'Python-мост (esp32_agent_bridge.py) использует многопоточную архитектуру: '
        'основной поток принимает подключения на Unix-сокете, для каждого клиента '
        'создаётся отдельный поток (threading.Thread с daemon=True). Доступ к '
        'серийному порту синхронизируется с помощью threading.Lock (self.serial_lock), '
        'что предотвращает одновременную отправку нескольких запросов к ESP32.',
        body_style
    )

    doc.add_paragraph('2.5.3 Go для клиентского приложения', subheading_style)

    doc.add_paragraph(
        'Язык Go был выбран для клиентского приложения по следующим причинам: '
        '(1) статическая компиляция — Go создаёт единый исполняемый файл без '
        'внешних зависимостей, что упрощает распространение; (2) богатая стандартная '
        'библиотека с поддержкой сетевых протоколов; (3) пакет golang.org/x/crypto/ssh '
        'предоставляет полноценную реализацию SSH-клиента; (4) пакет golang.org/x/term '
        'позволяет безопасно считывать пароль без отображения на экране (аналог getpass '
        'в Python); (5) высокая производительность и низкое потребление памяти.',
        body_style
    )

    doc.add_paragraph(
        'Go-клиент реализует две модели работы: (1) с файловым закрытым ключом '
        '(RSA PEM) — для обратной совместимости; (2) с ESP32-агентом через '
        'Unix-сокет — основной режим. Модуль go.mod определяет три зависимости: '
        'golang.org/x/crypto (SSH-клиент), golang.org/x/term (безопасный ввод пароля) '
        'и golang.org/x/sys (системные вызовы, транзитивная зависимость).',
        body_style
    )

    doc.add_paragraph('2.5.4 C для серверного менеджера', subheading_style)

    doc.add_paragraph(
        'Серверный менеджер реализован на языке C по следующим причинам: '
        '(1) прямой доступ к системным API Linux — mount(), fork(), cryptsetup — '
        'без промежуточных абстракций; (2) минимальные зависимости — OpenSSL '
        '(криптография), SQLCipher (зашифрованная БД), pthread (многопоточность); '
        '(3) низкий overhead по памяти и CPU, важный для серверного процесса; '
        '(4) C является стандартным языком системного программирования в экосистеме '
        'Linux и хорошо подходит для задач, связанных с управлением контейнерами '
        'и шифрованием дисков.',
        body_style
    )

    doc.add_paragraph('2.5.5 SQLCipher для хранения данных пользователей', subheading_style)

    doc.add_paragraph(
        'SQLCipher — это расширение SQLite, добавляющее прозрачное AES-256 шифрование '
        'всей базы данных. В системе SQLCipher используется для хранения следующих '
        'данных: таблица users (user_id TEXT PK, public_key_pem TEXT, public_key_ssh TEXT) — '
        'открытые ключи пользователей в формате PEM и SSH wire format; таблица containers '
        '(container_id TEXT PK, user_id TEXT FK, container_key BLOB) — ключи шифрования '
        'LUKS-томов контейнеров. Мастер-пароль базы данных передаётся через переменную '
        'окружения DB_PASSWORD и применяется командой PRAGMA key. Это обеспечивает '
        'защиту данных при несанкционированном доступе к файлу базы encrypted_users.db.',
        body_style
    )

    doc.add_paragraph('2.5.6 LUKS для шифрования домашних каталогов', subheading_style)

    doc.add_paragraph(
        'LUKS (Linux Unified Key Setup) используется для шифрования домашних '
        'каталогов пользователей в контейнерах. Для каждого контейнера создаётся '
        'файл-образ размером 100 МБ (./homes/<container_id>.img), который форматируется '
        'как LUKS-том командой cryptsetup luksFormat с 32-байтовым ключом из базы данных. '
        'При аутентификации пользователя том открывается (cryptsetup open), '
        'монтируется как ext4 и привязывается к контейнеру через disk device. '
        'При отключении пользователя том автоматически размонтируется и закрывается. '
        'Фоновый поток (cleanup_worker) каждые 10 секунд проверяет состояние контейнеров '
        'и закрывает тома остановленных контейнеров.',
        body_style
    )

    # Таблица 6 — Сводка технологий
    doc.add_paragraph(
        'Таблица 6 — Сводка используемых технологий и их назначение',
        caption_style
    )
    table6 = doc.add_table(rows=9, cols=3, style=table_style)
    headers6 = ['Компонент', 'Технология', 'Назначение']
    for i, h in enumerate(headers6):
        table6.rows[0].cells[i].text = h
    tech_data = [
        ('Аппаратный модуль', 'ESP32-C3 + Arduino/PlatformIO', 'Хранение ключей, подпись'),
        ('Криптобиблиотека ESP32', 'mbedTLS (ESP-IDF)', 'PBKDF2, AES-256-CBC, SHA-512'),
        ('Мост (SSH-агент)', 'Python 3 + pyserial', 'Трансляция протоколов'),
        ('Клиент', 'Go + x/crypto + x/term', 'Аутентификация, SSH'),
        ('Менеджер', 'C + OpenSSL + pthread', 'Управление контейнерами'),
        ('База данных', 'SQLCipher', 'Хранение ключей пользователей'),
        ('Шифрование томов', 'LUKS / dm-crypt', 'Шифрование домашних каталогов'),
        ('Контейнеризация', 'LXD / LXC (Alpine 3.20)', 'Изолированные среды'),
    ]
    for row_idx, (comp, tech, purpose) in enumerate(tech_data, 1):
        table6.rows[row_idx].cells[0].text = comp
        table6.rows[row_idx].cells[1].text = tech
        table6.rows[row_idx].cells[2].text = purpose

    doc.add_paragraph('')

    doc.add_paragraph(
        'Выводы по главе: в данной главе была описана архитектура системы, состоящей '
        'из пяти взаимодействующих компонентов. Разработан собственный бинарный протокол '
        'для взаимодействия с ESP32 через USB-serial, интегрированный с протоколом '
        'SSH-агента через механизм расширений. Модель безопасности обеспечивает '
        'многоуровневую защиту: от аппаратного хранения ключей до шифрования '
        'контейнерных томов. Выбор технологий обоснован требованиями к производительности, '
        'стоимости, безопасности и удобству разработки.',
        body_style
    )
